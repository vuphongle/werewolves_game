name: Android Release Build

on:
  push:
    tags:
      - 'v*' # Only build release when you push a tag like v1.0

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android
    permissions:
      contents: write # Required to create the Release

    steps:
    - uses: actions/checkout@v4

    - name: set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Set up Python 3.13
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    # Decode the secret back into a file
    - name: Decode Keystore
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > app/release.jks

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Signed Release APK
      run: ./gradlew assembleRelease
      env:
        VERSION_NAME: ${{ github.ref_name }}
        SIGNING_KEY_STORE_PATH: release.jks
        SIGNING_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        SIGNING_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        SIGNING_STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}

    - name: Rename APK with Version Tag
      run: |
        mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/app-release-${{ github.ref_name }}.apk

    - name: Release to GitHub
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: android/app/build/outputs/apk/release/app-release-${{ github.ref_name }}.apk
        draft: false
        prerelease: false
        generate_release_notes: true
